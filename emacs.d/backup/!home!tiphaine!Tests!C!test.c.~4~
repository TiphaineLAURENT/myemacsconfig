#include <stdio.h>

static size_t my_strlen(const char *str)
{
	return *str == 0 ? 0 : my_strlen(str + 1) + 1;
}

int main()
{
	printf("%ld", my_strlen("test"));
	printf("%ld", my_strlen(""));
	printf("%ld", my_strlen(0));
}
