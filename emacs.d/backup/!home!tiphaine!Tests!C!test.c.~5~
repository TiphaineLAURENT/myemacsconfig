#include <stdio.h>

static size_t my_strlen(const char *str)
{
	return str == NULL ? 0 : *str == 0 ? 0 : my_strlen(str + 1) + 1;
}

int main()
{
	printf("%ld\n", my_strlen("test"));
	printf("%ld\n", my_strlen(""));
	printf("%ld\n", my_strlen(0));
}
